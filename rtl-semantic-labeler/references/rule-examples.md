# Rule Examples

- awvalid + awready + wdata + bresp => axi_lite
- wr_ptr + rd_ptr + full + empty => fifo
- two-flop chain crossing clock domain => cdc synchronizer
- enum state + case(state) + always_ff => fsm-like controller
