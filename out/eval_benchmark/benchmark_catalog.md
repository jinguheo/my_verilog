# RTL QA Benchmark Catalog

## Easy (50)

1. Which module named `clkgen_xil7series` should be returned from the current RTL knowledge DB?
   - gold: `clkgen_xil7series` (ibex)
   - type: `exact_name`
2. Which module named `ibex_alu` should be returned from the current RTL knowledge DB?
   - gold: `ibex_alu` (ibex)
   - type: `exact_name`
3. Which module named `ibex_branch_predict` should be returned from the current RTL knowledge DB?
   - gold: `ibex_branch_predict` (ibex)
   - type: `exact_name`
4. Which module named `ibex_controller` should be returned from the current RTL knowledge DB?
   - gold: `ibex_controller` (ibex)
   - type: `exact_name`
5. Which module named `ibex_counter` should be returned from the current RTL knowledge DB?
   - gold: `ibex_counter` (ibex)
   - type: `exact_name`
6. Which module named `ibex_cs_registers` should be returned from the current RTL knowledge DB?
   - gold: `ibex_cs_registers` (ibex)
   - type: `exact_name`
7. Which module named `ibex_csr` should be returned from the current RTL knowledge DB?
   - gold: `ibex_csr` (ibex)
   - type: `exact_name`
8. Which module named `ibex_dummy_instr` should be returned from the current RTL knowledge DB?
   - gold: `ibex_dummy_instr` (ibex)
   - type: `exact_name`
9. Which module named `ibex_ex_block` should be returned from the current RTL knowledge DB?
   - gold: `ibex_ex_block` (ibex)
   - type: `exact_name`
10. Which module named `ibex_fetch_fifo` should be returned from the current RTL knowledge DB?
   - gold: `ibex_fetch_fifo` (ibex)
   - type: `exact_name`
11. Which module named `ibex_icache` should be returned from the current RTL knowledge DB?
   - gold: `ibex_icache` (ibex)
   - type: `exact_name`
12. Which module named `ibex_id_stage` should be returned from the current RTL knowledge DB?
   - gold: `ibex_id_stage` (ibex)
   - type: `exact_name`
13. Which module named `ibex_if_stage` should be returned from the current RTL knowledge DB?
   - gold: `ibex_if_stage` (ibex)
   - type: `exact_name`
14. Which module named `ibex_load_store_unit` should be returned from the current RTL knowledge DB?
   - gold: `ibex_load_store_unit` (ibex)
   - type: `exact_name`
15. Which module named `ibex_multdiv_fast` should be returned from the current RTL knowledge DB?
   - gold: `ibex_multdiv_fast` (ibex)
   - type: `exact_name`
16. Which module named `ibex_multdiv_slow` should be returned from the current RTL knowledge DB?
   - gold: `ibex_multdiv_slow` (ibex)
   - type: `exact_name`
17. Which module named `ibex_pmp` should be returned from the current RTL knowledge DB?
   - gold: `ibex_pmp` (ibex)
   - type: `exact_name`
18. Which module named `ibex_prefetch_buffer` should be returned from the current RTL knowledge DB?
   - gold: `ibex_prefetch_buffer` (ibex)
   - type: `exact_name`
19. Which module named `ibex_register_file_ff` should be returned from the current RTL knowledge DB?
   - gold: `ibex_register_file_ff` (ibex)
   - type: `exact_name`
20. Which module named `ibex_register_file_fpga` should be returned from the current RTL knowledge DB?
   - gold: `ibex_register_file_fpga` (ibex)
   - type: `exact_name`
21. Which module named `ibex_register_file_latch` should be returned from the current RTL knowledge DB?
   - gold: `ibex_register_file_latch` (ibex)
   - type: `exact_name`
22. Which module named `ibex_tracer` should be returned from the current RTL knowledge DB?
   - gold: `ibex_tracer` (ibex)
   - type: `exact_name`
23. Which module named `ibex_wb_stage` should be returned from the current RTL knowledge DB?
   - gold: `ibex_wb_stage` (ibex)
   - type: `exact_name`
24. Which module named `prim_and2` should be returned from the current RTL knowledge DB?
   - gold: `prim_and2` (ibex)
   - type: `exact_name`
25. Which module named `prim_arbiter_fixed` should be returned from the current RTL knowledge DB?
   - gold: `prim_arbiter_fixed` (ibex)
   - type: `exact_name`
26. Which module named `prim_ascon_duplex` should be returned from the current RTL knowledge DB?
   - gold: `prim_ascon_duplex` (ibex)
   - type: `exact_name`
27. Which module named `prim_ascon_duplex_tb` should be returned from the current RTL knowledge DB?
   - gold: `prim_ascon_duplex_tb` (ibex)
   - type: `exact_name`
28. Which module named `prim_ascon_round` should be returned from the current RTL knowledge DB?
   - gold: `prim_ascon_round` (ibex)
   - type: `exact_name`
29. Which module named `prim_ascon_round_tb` should be returned from the current RTL knowledge DB?
   - gold: `prim_ascon_round_tb` (ibex)
   - type: `exact_name`
30. Which module named `prim_ascon_sbox` should be returned from the current RTL knowledge DB?
   - gold: `prim_ascon_sbox` (ibex)
   - type: `exact_name`
31. Which module named `prim_buf` should be returned from the current RTL knowledge DB?
   - gold: `prim_buf` (ibex)
   - type: `exact_name`
32. Which module named `prim_clock_buf` should be returned from the current RTL knowledge DB?
   - gold: `prim_clock_buf` (ibex)
   - type: `exact_name`
33. Which module named `prim_clock_div` should be returned from the current RTL knowledge DB?
   - gold: `prim_clock_div` (ibex)
   - type: `exact_name`
34. Which module named `prim_clock_gating` should be returned from the current RTL knowledge DB?
   - gold: `prim_clock_gating` (ibex)
   - type: `exact_name`
35. Which module named `prim_clock_gating_sync` should be returned from the current RTL knowledge DB?
   - gold: `prim_clock_gating_sync` (ibex)
   - type: `exact_name`
36. Which module named `prim_clock_inv` should be returned from the current RTL knowledge DB?
   - gold: `prim_clock_inv` (ibex)
   - type: `exact_name`
37. Which module named `prim_clock_mux2` should be returned from the current RTL knowledge DB?
   - gold: `prim_clock_mux2` (ibex)
   - type: `exact_name`
38. Which module named `prim_count` should be returned from the current RTL knowledge DB?
   - gold: `prim_count` (ibex)
   - type: `exact_name`
39. Which module named `prim_crc32_sim` should be returned from the current RTL knowledge DB?
   - gold: `prim_crc32_sim` (ibex)
   - type: `exact_name`
40. Which module named `prim_dom_and_2share` should be returned from the current RTL knowledge DB?
   - gold: `prim_dom_and_2share` (ibex)
   - type: `exact_name`
41. Which module named `prim_edge_detector` should be returned from the current RTL knowledge DB?
   - gold: `prim_edge_detector` (ibex)
   - type: `exact_name`
42. Which module named `prim_fifo_async` should be returned from the current RTL knowledge DB?
   - gold: `prim_fifo_async` (ibex)
   - type: `exact_name`
43. Which module named `prim_fifo_async_simple` should be returned from the current RTL knowledge DB?
   - gold: `prim_fifo_async_simple` (ibex)
   - type: `exact_name`
44. Which module named `prim_fifo_async_sram_adapter` should be returned from the current RTL knowledge DB?
   - gold: `prim_fifo_async_sram_adapter` (ibex)
   - type: `exact_name`
45. Which module named `prim_fifo_sync` should be returned from the current RTL knowledge DB?
   - gold: `prim_fifo_sync` (ibex)
   - type: `exact_name`
46. Which module named `prim_fifo_sync_cnt` should be returned from the current RTL knowledge DB?
   - gold: `prim_fifo_sync_cnt` (ibex)
   - type: `exact_name`
47. Which module named `prim_filter` should be returned from the current RTL knowledge DB?
   - gold: `prim_filter` (ibex)
   - type: `exact_name`
48. Which module named `prim_filter_ctr` should be returned from the current RTL knowledge DB?
   - gold: `prim_filter_ctr` (ibex)
   - type: `exact_name`
49. Which module named `prim_flash` should be returned from the current RTL knowledge DB?
   - gold: `prim_flash` (ibex)
   - type: `exact_name`
50. Which module named `prim_flop` should be returned from the current RTL knowledge DB?
   - gold: `prim_flop` (ibex)
   - type: `exact_name`

## Medium (50)

1. Which `opentitan` module behaves like a `fifo` block and includes ports `mio_in_i` and `mio_out_o`?
   - gold: `top_earlgrey` (opentitan)
   - type: `label_plus_ports`
2. Find the module in `opentitan` that instantiates `pinmux_jtag_breakout` and is implemented at `top_earlgrey.sv`.
   - gold: `top_earlgrey` (opentitan)
   - type: `parent_child`
3. Which `opentitan` module behaves like a `apb` block and includes ports `mio_in_i` and `mio_out_o`?
   - gold: `top_englishbreakfast` (opentitan)
   - type: `label_plus_ports`
4. Find the module in `opentitan` that instantiates `pinmux_jtag_breakout` and is implemented at `top_englishbreakfast.sv`.
   - gold: `top_englishbreakfast` (opentitan)
   - type: `parent_child`
5. Which `opentitan` module behaves like a `sda` block and includes ports `mio_in_i` and `mio_out_o`?
   - gold: `top_darjeeling` (opentitan)
   - type: `label_plus_ports`
6. Find the module in `opentitan` that instantiates `uart` and is implemented at `top_darjeeling.sv`.
   - gold: `top_darjeeling` (opentitan)
   - type: `parent_child`
7. Which `opentitan` module behaves like a `serial bus` block and includes ports `POR_N` and `JTAG_TCK`?
   - gold: `chip_darjeeling_asic` (opentitan)
   - type: `label_plus_ports`
8. Find the module in `opentitan` that instantiates `padring` and is implemented at `chip_darjeeling_asic.sv`.
   - gold: `chip_darjeeling_asic` (opentitan)
   - type: `parent_child`
9. Which module belongs to the `chip_darjeeling_asic` path region and still exposes port `MIO11`?
   - gold: `chip_darjeeling_asic` (opentitan)
   - type: `path_plus_port`
10. Which `opentitan` module behaves like a `serial bus` block and includes ports `clk_peri_i` and `rst_peri_ni`?
   - gold: `generated` (opentitan)
   - type: `label_plus_ports`
11. Find the module in `opentitan` that instantiates `tlul_socket_1n` and is implemented at `xbar_peri.sv`.
   - gold: `generated` (opentitan)
   - type: `parent_child`
12. Which `opentitan` module behaves like a `buffer` block and includes ports `clk_i` and `rst_ni`?
   - gold: `i2c_target_fsm` (opentitan)
   - type: `label_plus_ports`
13. Which module belongs to the `i2c_target_fsm` path region and still exposes port `used`?
   - gold: `i2c_target_fsm` (opentitan)
   - type: `path_plus_port`
14. Which `opentitan` module behaves like a `queue` block and includes ports `clk_i` and `rst_ni`?
   - gold: `i2c_controller_fsm` (opentitan)
   - type: `label_plus_ports`
15. Which module belongs to the `i2c_controller_fsm` path region and still exposes port `synchronizer`?
   - gold: `i2c_controller_fsm` (opentitan)
   - type: `path_plus_port`
16. Which `opentitan` module behaves like a `buffer` block and includes ports `clk_i` and `rst_ni`?
   - gold: `spi_device` (opentitan)
   - type: `label_plus_ports`
17. Find the module in `opentitan` that instantiates `prim_racl_error_arb` and is implemented at `spi_device.sv`.
   - gold: `spi_device` (opentitan)
   - type: `parent_child`
18. Which `opentitan` module behaves like a `queue` block and includes ports `clk_i` and `rst_ni`?
   - gold: `sha3` (opentitan)
   - type: `label_plus_ports`
19. Find the module in `opentitan` that instantiates `sha3pad` and is implemented at `sha3.sv`.
   - gold: `sha3` (opentitan)
   - type: `parent_child`
20. Which module belongs to the `sha3` path region and still exposes port `endcase`?
   - gold: `sha3` (opentitan)
   - type: `path_plus_port`
21. Which `opentitan` module behaves like a `buffer` block and includes ports `clk_i` and `rst_ni`?
   - gold: `i2c_fifos` (opentitan)
   - type: `label_plus_ports`
22. Find the module in `opentitan` that instantiates `i2c_fifo_sync_sram_adapter` and is implemented at `i2c_fifos.sv`.
   - gold: `i2c_fifos` (opentitan)
   - type: `parent_child`
23. Which module belongs to the `i2c_fifos` path region and still exposes port `acq_fifo_rdata_o`?
   - gold: `i2c_fifos` (opentitan)
   - type: `path_plus_port`
24. Which `opentitan` module behaves like a `apb` block and includes ports `clk_i` and `rst_ni`?
   - gold: `otp_ctrl` (opentitan)
   - type: `label_plus_ports`
25. Find the module in `opentitan` that instantiates `otp_ctrl_core_reg_top` and is implemented at `otp_ctrl.sv`.
   - gold: `otp_ctrl` (opentitan)
   - type: `parent_child`
26. Which `opentitan` module behaves like a `serial bus` block and includes ports `clk_i` and `rst_ni`?
   - gold: `soc_proxy` (opentitan)
   - type: `label_plus_ports`
27. Find the module in `opentitan` that instantiates `tlul_socket_m1` and is implemented at `soc_proxy.sv`.
   - gold: `soc_proxy` (opentitan)
   - type: `parent_child`
28. Which `opentitan` module behaves like a `fifo` block and includes ports `clk_i` and `rst_ni`?
   - gold: `i2c_core` (opentitan)
   - type: `label_plus_ports`
29. Find the module in `opentitan` that instantiates `i2c_fifos` and is implemented at `i2c_core.sv`.
   - gold: `i2c_core` (opentitan)
   - type: `parent_child`
30. Which module belongs to the `i2c_core` path region and still exposes port `change`?
   - gold: `i2c_core` (opentitan)
   - type: `path_plus_port`
31. Which `opentitan` module behaves like a `queue` block and includes ports `clk_i` and `rst_ni`?
   - gold: `spi_host_data_fifos` (opentitan)
   - type: `label_plus_ports`
32. Find the module in `opentitan` that instantiates `prim_fifo_sync` and is implemented at `spi_host_data_fifos.sv`.
   - gold: `spi_host_data_fifos` (opentitan)
   - type: `parent_child`
33. Which module belongs to the `spi_host_data_fifos` path region and still exposes port `rx_wm_o`?
   - gold: `spi_host_data_fifos` (opentitan)
   - type: `path_plus_port`
34. Which `opentitan` module behaves like a `queue` block and includes ports `clk_i` and `rst_ni`?
   - gold: `spi_host` (opentitan)
   - type: `label_plus_ports`
35. Find the module in `opentitan` that instantiates `prim_racl_error_arb` and is implemented at `spi_host.sv`.
   - gold: `spi_host` (opentitan)
   - type: `parent_child`
36. Which `opentitan` module behaves like a `serial bus` block and includes ports `clk_i` and `rst_ni`?
   - gold: `chip_darjeeling_verilator` (opentitan)
   - type: `label_plus_ports`
37. Find the module in `opentitan` that instantiates `prim_clock_div` and is implemented at `chip_darjeeling_verilator.sv`.
   - gold: `chip_darjeeling_verilator` (opentitan)
   - type: `parent_child`
38. Which module belongs to the `chip_darjeeling_verilator` path region and still exposes port `cio_spi_device_sdo_en_d2p_o`?
   - gold: `chip_darjeeling_verilator` (opentitan)
   - type: `path_plus_port`
39. Which `opentitan` module behaves like a `buffer` block and includes ports `clk_i` and `rst_ni`?
   - gold: `spi_host_window` (opentitan)
   - type: `label_plus_ports`
40. Find the module in `opentitan` that instantiates `tlul_adapter_reg_racl` and is implemented at `spi_host_window.sv`.
   - gold: `spi_host_window` (opentitan)
   - type: `parent_child`
41. Which module belongs to the `spi_host_window` path region and still exposes port `top_racl_pkg`?
   - gold: `spi_host_window` (opentitan)
   - type: `path_plus_port`
42. Which `opentitan` module behaves like a `fifo` block and includes ports `clk_i` and `rst_ni`?
   - gold: `spi_p2s` (opentitan)
   - type: `label_plus_ports`
43. Which module belongs to the `spi_p2s` path region and still exposes port `io_mode_e`?
   - gold: `spi_p2s` (opentitan)
   - type: `path_plus_port`
44. Which `opentitan` module behaves like a `serial bus` block and includes ports `isters` and `clk_i`?
   - gold: `controls` (opentitan)
   - type: `label_plus_ports`
45. Find the module in `opentitan` that instantiates `aes_control_fsm_p` and is implemented at `aes_control.sv`.
   - gold: `controls` (opentitan)
   - type: `parent_child`
46. Which module belongs to the `aes_control` path region and still exposes port `bit`?
   - gold: `controls` (opentitan)
   - type: `path_plus_port`
47. Which `opentitan` module behaves like a `buffer` block and includes ports `clk_48mhz_i` and `rst_ni`?
   - gold: `runs` (opentitan)
   - type: `label_plus_ports`
48. Find the module in `opentitan` that instantiates `usb_fs_nb_pe` and is implemented at `usbdev_usbif.sv`.
   - gold: `runs` (opentitan)
   - type: `parent_child`
49. Which module belongs to the `usbdev_usbif` path region and still exposes port `event_nodata_in_o`?
   - gold: `runs` (opentitan)
   - type: `path_plus_port`
50. Which `opentitan` module behaves like a `queue` block and includes ports `clk_48mhz_i` and `rst_ni`?
   - gold: `usb_fs_nb_pe` (opentitan)
   - type: `label_plus_ports`

## Hard (50)

1. Which parent module in `ibex` wraps child `ibex_csr` and also looks like a `apb` / `clocked` block?
   - gold: `ibex_cs_registers` (ibex)
   - type: `reverse_graph`
2. Which parent module in `opentitan` wraps child `ast_alert` and also looks like a `sequential` / `hierarchical` block?
   - gold: `ast` (opentitan)
   - type: `reverse_graph`
3. Which parent module in `opentitan` wraps child `entropy_src_cntr_reg` and also looks like a `clocked` / `queue` block?
   - gold: `entropy_src_core` (opentitan)
   - type: `reverse_graph`
4. Which parent module in `opentitan` wraps child `clkmgr_root_ctrl` and also looks like a `clocked` / `wrapper` block?
   - gold: `clkmgr` (opentitan)
   - type: `reverse_graph`
5. Which parent module in `opentitan` wraps child `rv_core_ibex_addr_trans` and also looks like a `apb` / `clocked` block?
   - gold: `rv_core_ibex` (opentitan)
   - type: `reverse_graph`
6. Which parent module in `opentitan` wraps child `clkmgr_meas_chk` and also looks like a `clocked` / `parent block` block?
   - gold: `clkmgr` (opentitan)
   - type: `reverse_graph`
7. Which parent module in `opentitan` wraps child `entropy_src_watermark_reg` and also looks like a `sequential` / `buffer` block?
   - gold: `entropy_src_core` (opentitan)
   - type: `reverse_graph`
8. Which parent module in `opentitan` wraps child `mbx` and also looks like a `clocked` / `hierarchical` block?
   - gold: `top_darjeeling` (opentitan)
   - type: `reverse_graph`
9. Which parent module in `opentitan` wraps child `tlul_fifo_async` and also looks like a `clocked` / `fifo` block?
   - gold: `generated` (opentitan)
   - type: `reverse_graph`
10. Which parent module in `opentitan` wraps child `clkmgr_trans` and also looks like a `clocked` / `parent block` block?
   - gold: `clkmgr` (opentitan)
   - type: `reverse_graph`
11. Which parent module in `opentitan` wraps child `pinmux_jtag_buf` and also looks like a `apb` / `clocked` block?
   - gold: `pinmux_strap_sampling` (opentitan)
   - type: `reverse_graph`
12. Which parent module in `opentitan` wraps child `clkmgr_clk_status` and also looks like a `clocked` / `wrapper` block?
   - gold: `clkmgr` (opentitan)
   - type: `reverse_graph`
13. Which parent module in `ibex` wraps child `ibex_counter` and also looks like a `apb` / `sequential` block?
   - gold: `ibex_cs_registers` (ibex)
   - type: `reverse_graph`
14. Which parent module in `opentitan` wraps child `prim_pad_attr` and also looks like a `sequential` / `parent block` block?
   - gold: `pinmux` (opentitan)
   - type: `reverse_graph`
15. Which parent module in `opentitan` wraps child `prim_secded_hamming_72_64_enc` and also looks like a `sequential` / `fifo` block?
   - gold: `implements` (opentitan)
   - type: `reverse_graph`
16. Which parent module in `opentitan` wraps child `rv_core_ibex_cfg_reg_top` and also looks like a `apb` / `clocked` block?
   - gold: `rv_core_ibex` (opentitan)
   - type: `reverse_graph`
17. Which parent module in `opentitan` wraps child `prim_double_lfsr` and also looks like a `clocked` / `parent block` block?
   - gold: `implements` (opentitan)
   - type: `reverse_graph`
18. Which parent module in `opentitan` wraps child `gfr_clk_mux2` and also looks like a `clocked` / `hierarchical` block?
   - gold: `ast_clks_byp` (opentitan)
   - type: `reverse_graph`
19. Which parent module in `opentitan` wraps child `i2c_fifo_sync_sram_adapter` and also looks like a `sequential` / `buffer` block?
   - gold: `i2c_fifos` (opentitan)
   - type: `reverse_graph`
20. Which parent module in `opentitan` wraps child `otp_ctrl_part_buf` and also looks like a `apb` / `sequential` block?
   - gold: `otp_ctrl` (opentitan)
   - type: `reverse_graph`
21. Find the `opentitan` wrapper that behaves like `sequential`, instantiates both `prim_mubi4_sync` and `prim_mubi4_sync`, and still exposes `clk_i`.
   - gold: `entropy_src_core` (opentitan)
   - type: `multi_hop`
22. Find the `opentitan` wrapper that behaves like `sequential`, instantiates both `prim_clock_buf` and `prim_clock_buf`, and still exposes `clk_i`.
   - gold: `clkmgr` (opentitan)
   - type: `multi_hop`
23. Find the `opentitan` wrapper that behaves like `clocked`, instantiates both `prim_clock_buf` and `prim_flop_2sync`, and still exposes `vcaon_pok_i`.
   - gold: `ast_clks_byp` (opentitan)
   - type: `multi_hop`
24. Find the `opentitan` wrapper that behaves like `sequential`, instantiates both `prim_racl_error_arb` and `prim_buf`, and still exposes `clk_i`.
   - gold: `spi_device` (opentitan)
   - type: `multi_hop`
25. Find the `opentitan` wrapper that behaves like `clocked`, instantiates both `uart` and `gpio`, and still exposes `mio_in_i`.
   - gold: `top_darjeeling` (opentitan)
   - type: `multi_hop`
26. Find the `opentitan` wrapper that behaves like `sequential`, instantiates both `tlul_socket_1n` and `tlul_socket_m1`, and still exposes `clk_main_i`.
   - gold: `generated` (opentitan)
   - type: `multi_hop`
27. Find the `opentitan` wrapper that behaves like `clocked`, instantiates both `pinmux_jtag_breakout` and `uart`, and still exposes `mio_in_i`.
   - gold: `top_earlgrey` (opentitan)
   - type: `multi_hop`
28. Find the `opentitan` wrapper that behaves like `sequential`, instantiates both `prim_flop` and `prim_flop`, and still exposes `clk_i`.
   - gold: `dma` (opentitan)
   - type: `multi_hop`
29. Find the `opentitan` wrapper that behaves like `sequential`, instantiates both `prim_clock_buf` and `prim_clock_buf`, and still exposes `tlul_pkg`.
   - gold: `ast` (opentitan)
   - type: `multi_hop`
30. Find the `opentitan` wrapper that behaves like `apb`, instantiates both `otp_ctrl_core_reg_top` and `prim_lc_sync`, and still exposes `clk_i`.
   - gold: `otp_ctrl` (opentitan)
   - type: `multi_hop`
31. Find the `opentitan` wrapper that behaves like `apb`, instantiates both `prim_esc_receiver` and `prim_flop_2sync`, and still exposes `clk_i`.
   - gold: `rv_core_ibex` (opentitan)
   - type: `multi_hop`
32. Find the `opentitan` wrapper that behaves like `sequential`, instantiates both `prim_fifo_sync` and `prim_fifo_sync`, and still exposes `clk_i`.
   - gold: `usbdev` (opentitan)
   - type: `multi_hop`
33. Find the `opentitan` wrapper that behaves like `clocked`, instantiates both `flash_ctrl_core_reg_top` and `flash_ctrl_region_cfg`, and still exposes `clk_i`.
   - gold: `flash_ctrl` (opentitan)
   - type: `multi_hop`
34. Find the `opentitan` wrapper that behaves like `clocked`, instantiates both `prim_onehot_mux` and `prim_onehot_mux`, and still exposes `clk_i`.
   - gold: `otbn_alu_bignum` (opentitan)
   - type: `multi_hop`
35. Find the `opentitan` wrapper that behaves like `apb`, instantiates both `ibex_csr` and `ibex_csr`, and still exposes `clk_i`.
   - gold: `ibex_cs_registers` (opentitan)
   - type: `multi_hop`
36. Which module would best answer a query for a `parent block` bridge that synchronizes around child `prim_buf` in `opentitan`?
   - gold: `entropy_src_core` (opentitan)
   - type: `semantic_bridge`
37. Which module would best answer a query for a `tx` bridge that synchronizes around child `clkmgr_trans` in `opentitan`?
   - gold: `clkmgr` (opentitan)
   - type: `semantic_bridge`
38. Which module would best answer a query for a `sequential` bridge that synchronizes around child `prim_alert_sender` in `opentitan`?
   - gold: `spi_device` (opentitan)
   - type: `semantic_bridge`
39. Which module would best answer a query for a `i2c` bridge that synchronizes around child `xbar_dbg` in `opentitan`?
   - gold: `top_darjeeling` (opentitan)
   - type: `semantic_bridge`
40. Which module would best answer a query for a `sequential` bridge that synchronizes around child `tlul_socket_1n` in `opentitan`?
   - gold: `generated` (opentitan)
   - type: `semantic_bridge`
41. Which module would best answer a query for a `clocked` bridge that synchronizes around child `xbar_peri` in `opentitan`?
   - gold: `top_earlgrey` (opentitan)
   - type: `semantic_bridge`
42. Which module would best answer a query for a `resettable` bridge that synchronizes around child `prim_flop` in `opentitan`?
   - gold: `dma` (opentitan)
   - type: `semantic_bridge`
43. Which module would best answer a query for a `wrapper` bridge that synchronizes around child `ast_dft` in `opentitan`?
   - gold: `ast` (opentitan)
   - type: `semantic_bridge`
44. Which module would best answer a query for a `wrapper` bridge that synchronizes around child `prim_lc_sender` in `opentitan`?
   - gold: `otp_ctrl` (opentitan)
   - type: `semantic_bridge`
45. Which module would best answer a query for a `reset aware` bridge that synchronizes around child `tlul_err_resp` in `opentitan`?
   - gold: `rv_core_ibex` (opentitan)
   - type: `semantic_bridge`
46. Which module would best answer a query for a `reset aware` bridge that synchronizes around child `usbdev_counter` in `opentitan`?
   - gold: `usbdev` (opentitan)
   - type: `semantic_bridge`
47. Which module would best answer a query for a `resettable` bridge that synchronizes around child `tlul_rsp_intg_chk` in `opentitan`?
   - gold: `flash_ctrl` (opentitan)
   - type: `semantic_bridge`
48. Which module would best answer a query for a `clocked` bridge that synchronizes around child `otbn_vec_transposer` in `opentitan`?
   - gold: `otbn_alu_bignum` (opentitan)
   - type: `semantic_bridge`
49. Which module would best answer a query for a `resettable` bridge that synchronizes around child `ibex_csr` in `opentitan`?
   - gold: `ibex_cs_registers` (opentitan)
   - type: `semantic_bridge`
50. Which module would best answer a query for a `wrapper` bridge that synchronizes around child `ibex_csr` in `ibex`?
   - gold: `ibex_cs_registers` (ibex)
   - type: `semantic_bridge`
