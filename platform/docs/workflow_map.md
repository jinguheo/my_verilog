# Workflow map

build knowledge base:
ingest -> label -> relational load -> graph load -> review queue

answer questions:
sql exact -> graph traversal -> vector fallback -> response

generate:
structured spec -> ontology filter -> retrieve references -> architecture plan -> rtl skeleton
